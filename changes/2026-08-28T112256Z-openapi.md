# openapi changed

- url: https://cursor.com/docs/api/origin/openapi.yaml
- fetched: 2026-08-28T11:22:56Z
- lines: +1884/-264
- note: OpenAPI 3.1 spec for the Origin REST API (v1alpha1). Breaking changes land here.

````diff
--- a/snapshots/openapi.yaml
+++ b/snapshots/openapi.yaml
@@ -6,6 +6,8 @@
     title: Cursor Origin API
     description: Public third-party-facing Origin API.
     version: v1alpha1
+servers:
+    - url: https://api.cursor.com
 paths:
     /v1/origin/app:
         get:
@@ -20,6 +22,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/App'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -55,6 +81,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListAppInstallationsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -81,6 +131,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/AppInstallation'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -105,6 +179,30 @@
                 "204":
                     description: OK
                     content: {}
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -162,6 +260,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/InstallationAccessToken'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -175,8 +297,8 @@
             description: |-
                 Lists webhook deliveries for the authenticated app, newest first.
 
-                 A delivery is one event owed to one app; its id is the Standard Webhooks
-                 `webhook-id` header the receiver sees. `delivered=false` is the recovery
+                 A delivery is one event owed to one app; its id is the `webhook-id`
+                 header value the receiver sees. `delivered=false` is the recovery
                  predicate: every delivery the receiver has never acknowledged with a 2xx,
                  regardless of where retries stand.
 
@@ -235,6 +357,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListWebhookDeliveriesResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -269,6 +415,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/BatchRedeliverWebhookDeliveriesResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -303,6 +473,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PingWebhookResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -342,6 +536,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListAppInstallationRepositoriesResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -366,26 +584,175 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/RateLimitStatus'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}:
+    /v1/origin/repos/{ownerSlug}:
+        get:
+            tags:
+                - OriginService
+            description: Lists repos belonging to an owner entity.
+            operationId: OriginService_ListRepos
+            parameters:
+                - name: ownerSlug
+                  in: path
+                  description: Parent owner entity slug.
+                  required: true
+                  schema:
+                    type: string
+                - name: pageSize
+                  in: query
+                  description: |-
+                    Max repos to return. Defaults to 30 when unset or 0. Values above 100 are
+                     clamped to 100.
+                  schema:
+                    type: integer
+                    format: int32
+                - name: pageToken
+                  in: query
+                  description: |-
+                    Opaque cursor from a previous response's `next_page_token`. Empty for the
+                     first page.
+                  schema:
+                    type: string
+                - name: filter
+                  in: query
+                  description: Optional case-insensitive substring filter.
+                  schema:
+                    type: string
+            responses:
+                "200":
+                    description: OK
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/ListReposResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                default:
+                    description: Default error response
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+        post:
+            tags:
+                - OriginService
+            description: Creates a repo belonging to an owner.
+            operationId: OriginService_CreateRepo
+            parameters:
+                - name: ownerSlug
+                  in: path
+                  description: Parent owner entity's slug.
+                  required: true
+                  schema:
+                    type: string
+            requestBody:
+                content:
+                    application/json:
+                        schema:
+                            $ref: '#/components/schemas/Repo'
+                required: true
+            responses:
+                "200":
+                    description: OK
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Repo'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                default:
+                    description: Default error response
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+    /v1/origin/repos/{ownerSlug}/{repoName}:
         get:
             tags:
                 - OriginService
             description: Returns a single repo by its `(owner_id, name)` identifier.
             operationId: OriginService_GetRepo
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -398,13 +765,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Repo'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/branches:
+    /v1/origin/repos/{ownerSlug}/{repoName}/branches:
         get:
             tags:
                 - OriginService
@@ -413,13 +804,13 @@
                  paginated with `page_size` and `page_token`.
             operationId: OriginService_ListBranches
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -448,13 +839,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListBranchesResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/check-runs:
+    /v1/origin/repos/{ownerSlug}/{repoName}/check-runs:
         post:
             tags:
                 - OriginService
@@ -466,13 +881,13 @@
                  place rather than creating a duplicate.
             operationId: OriginService_PostCheckRun
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -507,26 +922,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PostCheckRunResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/check-runs/{checkRunId}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/check-runs/{checkRunId}:
         get:
             tags:
                 - OriginService
             description: Returns a single check run by server-assigned id (`cr_…`).
             operationId: OriginService_GetCheckRun
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -545,13 +984,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/CheckRun'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/check-runs/{checkRunId}/annotations:
+    /v1/origin/repos/{ownerSlug}/{repoName}/check-runs/{checkRunId}/annotations:
         get:
             tags:
                 - OriginService
@@ -561,13 +1024,13 @@
                  30 and maximum of 100.
             operationId: OriginService_ListCheckRunAnnotations
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -600,6 +1063,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListCheckRunAnnotationsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -618,13 +1105,13 @@
                  Identical content is intentionally allowed.
             operationId: OriginService_CreateCheckRunAnnotations
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -660,13 +1147,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/CreateCheckRunAnnotationsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/check-runs:batchUpsert:
+    /v1/origin/repos/{ownerSlug}/{repoName}/check-runs:batchUpsert:
         post:
             tags:
                 - OriginService
@@ -676,13 +1187,13 @@
                  identities. Every run is committed or the entire request is rolled back.
             operationId: OriginService_BatchUpsertCheckRuns
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -720,13 +1231,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/BatchUpsertCheckRunsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/check-suites/{checkSuiteId}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/check-suites/{checkSuiteId}:
         get:
             tags:
                 - OriginService
@@ -735,13 +1270,13 @@
                  embed check runs; use `ListCheckRunsForSuite` for the suite's runs.
             operationId: OriginService_GetCheckSuite
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -760,26 +1295,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/CheckSuite'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/check-suites/{checkSuiteId}/check-runs:
+    /v1/origin/repos/{ownerSlug}/{repoName}/check-suites/{checkSuiteId}/check-runs:
         get:
             tags:
                 - OriginService
             description: Lists check runs belonging to a suite. Paginated.
             operationId: OriginService_ListCheckRunsForSuite
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -814,26 +1373,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListCheckRunsForSuiteResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/commits:
+    /v1/origin/repos/{ownerSlug}/{repoName}/commits:
         get:
             tags:
                 - OriginService
             description: Lists commits on a branch or starting ref.
             operationId: OriginService_ListCommits
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -869,26 +1452,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListCommitsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/commits/{sha}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/commits/{sha}:
         get:
             tags:
                 - OriginService
             description: Returns a single commit by SHA or ref with whole-commit aggregate stats.
             operationId: OriginService_GetCommit
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -909,13 +1516,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Commit'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/commits/{sha}/check-runs:
+    /v1/origin/repos/{ownerSlug}/{repoName}/commits/{sha}/check-runs:
         get:
             tags:
                 - OriginService
@@ -924,13 +1555,13 @@
                  Paginated.
             operationId: OriginService_ListCheckRunsForCommit
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -965,13 +1596,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListCheckRunsForCommitResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/commits/{sha}/check-suites:
+    /v1/origin/repos/{ownerSlug}/{repoName}/commits/{sha}/check-suites:
         get:
             tags:
                 - OriginService
@@ -980,13 +1635,13 @@
                  only (no embedded runs). Paginated.
             operationId: OriginService_ListCheckSuitesForCommit
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1021,26 +1676,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListCheckSuitesForCommitResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/commits/{sha}/files:
+    /v1/origin/repos/{ownerSlug}/{repoName}/commits/{sha}/files:
         get:
             tags:
                 - OriginService
             description: Lists the files changed by a commit.
             operationId: OriginService_ListCommitFiles
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1077,13 +1756,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListCommitFilesResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/compare/{basehead}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/compare/{basehead}:
         get:
             tags:
                 - OriginService
@@ -1092,13 +1795,13 @@
                  `basehead` is `"{base}...{head}"`; refs containing "/" must use their SHA.
             operationId: OriginService_CompareCommits
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1119,13 +1822,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/CommitComparison'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/contents:
+    /v1/origin/repos/{ownerSlug}/{repoName}/contents:
         get:
             tags:
                 - OriginService
@@ -1137,13 +1864,13 @@
                  (HTTP 400); fetch larger files by cloning the repository over Git HTTPS.
             operationId: OriginService_GetContents
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1170,13 +1897,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Content'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/contents:batchGet:
+    /v1/origin/repos/{ownerSlug}/{repoName}/contents:batchGet:
         post:
             tags:
                 - OriginService
@@ -1191,13 +1942,13 @@
                  list travels in the request body.
             operationId: OriginService_BatchGetContents
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1232,13 +1983,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/BatchGetContentsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/git/blobs/{sha}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/git/blobs/{sha}:
         get:
             tags:
                 - OriginService
@@ -1252,13 +2027,13 @@
                  Conflict.
             operationId: OriginService_GetBlob
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1277,13 +2052,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Blob'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/git/commits/{sha}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/git/commits/{sha}:
         get:
             tags:
                 - OriginService
@@ -1295,13 +2094,13 @@
                  Empty repositories return 409 Conflict.
             operationId: OriginService_GetGitCommit
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1322,13 +2121,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/GitCommit'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/git/matching-refs:
+    /v1/origin/repos/{ownerSlug}/{repoName}/git/matching-refs:
         get:
             tags:
                 - OriginService
@@ -1340,13 +2163,13 @@
                  Empty repositories return 409 Conflict.
             operationId: OriginService_ListMatchingGitRefs
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1367,13 +2190,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListMatchingGitRefsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/git/matching-refs/{ref}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/git/matching-refs/{ref}:
         get:
             tags:
                 - OriginService
@@ -1385,13 +2232,13 @@
                  Empty repositories return 409 Conflict.
             operationId: OriginService_ListMatchingGitRefs
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1413,13 +2260,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListMatchingGitRefsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/git/ref/{ref}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/git/ref/{ref}:
         get:
             tags:
                 - OriginService
@@ -1430,13 +2301,13 @@
                  ListMatchingGitRefs for prefixes. Empty repositories return 409 Conflict.
             operationId: OriginService_GetGitRef
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1459,13 +2330,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/GitRef'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/git/tags/{sha}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/git/tags/{sha}:
         get:
             tags:
                 - OriginService
@@ -1475,13 +2370,13 @@
                  Empty repositories return 409 Conflict.
             operationId: OriginService_GetTag
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1500,13 +2395,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/GitTag'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/git/trees/{sha}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/git/trees/{sha}:
         get:
             tags:
                 - OriginService
@@ -1520,13 +2439,13 @@
                  return 409 Conflict.
             operationId: OriginService_GetTree
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1553,13 +2472,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/GitTree'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/labels:
+    /v1/origin/repos/{ownerSlug}/{repoName}/labels:
         get:
             tags:
                 - OriginService
@@ -1568,13 +2511,13 @@
                  `page_size` and `page_token`. Default page size is 30; maximum is 100.
             operationId: OriginService_ListLabels
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1602,6 +2545,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListLabelsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -1616,13 +2583,13 @@
                  `repository:labels:write`. Duplicate names return ALREADY_EXISTS.
             operationId: OriginService_CreateLabel
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1656,26 +2623,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Label'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/labels/{labelName}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/labels/{labelName}:
         get:
             tags:
                 - OriginService
             description: Returns a single repository label by name. Unknown names return NOT_FOUND.
             operationId: OriginService_GetLabel
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1694,6 +2685,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Label'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -1706,13 +2721,13 @@
             description: Deletes a repository label by name. Requires `repository:labels:write`.
             operationId: OriginService_DeleteLabel
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1728,6 +2743,30 @@
                 "204":
                     description: OK
                     content: {}
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -1742,13 +2781,13 @@
                  are left unchanged. Rename with `name`. Requires `repository:labels:write`.
             operationId: OriginService_UpdateLabel
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1787,13 +2826,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Label'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls:
         get:
             tags:
                 - OriginService
@@ -1804,13 +2867,13 @@
                  pull request includes its assigned labels.
             operationId: OriginService_ListPullRequests
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1883,6 +2946,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListPullRequestsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -1899,13 +2986,13 @@
                  pull request in the same repository.
             operationId: OriginService_CreatePullRequest
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1955,26 +3042,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PullRequest'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/comments/{commentId}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/comments/{commentId}:
         get:
             tags:
                 - OriginService
             description: Returns a single pull request comment by its stable Origin id.
             operationId: OriginService_GetPullRequestComment
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -1992,6 +3103,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PullRequestComment'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -2004,13 +3139,13 @@
             description: Updates a pull request comment by its stable Origin id.
             operationId: OriginService_UpdatePullRequestComment
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2039,26 +3174,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PullRequestComment'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}:
         get:
             tags:
                 - OriginService
             description: Returns a single pull request, including its assigned labels.
             operationId: OriginService_GetPullRequest
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2076,6 +3235,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PullRequest'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -2095,13 +3278,13 @@
                  fails, earlier steps may already have been committed.
             operationId: OriginService_UpdatePullRequest
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2149,13 +3332,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PullRequest'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}/comments:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}/comments:
         get:
             tags:
                 - OriginService
@@ -2164,13 +3371,13 @@
                  includes its thread id so clients can group the flat response into threads.
             operationId: OriginService_ListPullRequestComments
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2199,6 +3406,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListPullRequestCommentsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -2215,13 +3446,13 @@
                  comments.
             operationId: OriginService_CreatePullRequestComment
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2255,26 +3486,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PullRequestComment'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}/commits:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}/commits:
         get:
             tags:
                 - OriginService
             description: Lists the commits in a pull request.
             operationId: OriginService_ListPullRequestCommits
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2308,26 +3563,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListPullRequestCommitsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}/files:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}/files:
         get:
             tags:
                 - OriginService
             description: Lists the files changed in a pull request.
             operationId: OriginService_ListPullRequestFiles
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2361,13 +3640,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListPullRequestFilesResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}/labels:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}/labels:
         get:
             tags:
                 - OriginService
@@ -2377,13 +3680,13 @@
                  labels.
             operationId: OriginService_ListPullRequestLabels
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2401,6 +3704,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListPullRequestLabelsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -2417,13 +3744,13 @@
                  replacement, ordered by name.
             operationId: OriginService_SetPullRequestLabels
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2455,6 +3782,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/SetPullRequestLabelsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -2467,13 +3818,13 @@
             description: Adds existing repository labels to a pull request.
             operationId: OriginService_AddPullRequestLabels
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2505,6 +3856,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/AddPullRequestLabelsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -2520,13 +3895,13 @@
                  returns NOT_FOUND.
             operationId: OriginService_RemoveAllPullRequestLabels
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2541,13 +3916,37 @@
                 "204":
                     description: OK
                     content: {}
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}/labels/{labelName}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}/labels/{labelName}:
         delete:
             tags:
                 - OriginService
@@ -2556,13 +3955,13 @@
                  Returns the labels remaining on the pull request.
             operationId: OriginService_RemovePullRequestLabel
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2585,13 +3984,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/RemovePullRequestLabelResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}/merge:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}/merge:
         post:
             tags:
                 - OriginService
@@ -2603,13 +4026,13 @@
                  repositories; mirrored repositories are rejected.
             operationId: OriginService_MergePullRequest
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2647,13 +4070,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/MergePullRequestResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}/requested_reviewers:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}/requested_reviewers:
         get:
             tags:
                 - OriginService
@@ -2664,13 +4111,13 @@
                  returns them to this list.
             operationId: OriginService_ListPullRequestRequestedReviewers
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2688,13 +4135,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListPullRequestRequestedReviewersResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}/reviews:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}/reviews:
         get:
             tags:
                 - OriginService
@@ -2703,13 +4174,13 @@
                  ascending. Pending reviews are omitted.
             operationId: OriginService_ListPullRequestReviews
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2738,6 +4209,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListPullRequestReviewsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -2757,13 +4252,13 @@
                  unsubmitted draft review on the pull request.
             operationId: OriginService_CreatePullRequestReview
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2784,7 +4279,6 @@
                             properties:
                                 verdict:
                                     enum:
-                                        - PULL_REQUEST_REVIEW_VERDICT_UNSPECIFIED
                                         - approve
                                         - request_changes
                                         - comment
@@ -2808,13 +4302,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PullRequestReview'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}/reviews/{reviewId}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}/reviews/{reviewId}:
         patch:
             tags:
                 - OriginService
@@ -2827,13 +4345,13 @@
                  `submitted_at`.
             operationId: OriginService_UpdatePullRequestReview
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2870,13 +4388,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PullRequestReview'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/pulls/{pullNumber}/reviews/{reviewId}/dismissals:
+    /v1/origin/repos/{ownerSlug}/{repoName}/pulls/{pullNumber}/reviews/{reviewId}/dismissals:
         put:
             tags:
                 - OriginService
@@ -2895,13 +4437,13 @@
                  the named pull request returns NOT_FOUND.
             operationId: OriginService_DismissPullRequestReview
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2939,13 +4481,37 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/PullRequestReview'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/rulesets:
+    /v1/origin/repos/{ownerSlug}/{repoName}/rulesets:
         get:
             tags:
                 - OriginService
@@ -2954,13 +4520,13 @@
                  one response (rulesets per repository are bounded configuration).
             operationId: OriginService_ListRulesets
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -2973,6 +4539,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/ListRulesetsResponse'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -2987,13 +4577,13 @@
                  server-assigned rule and bypass-actor ids.
             operationId: OriginService_CreateRuleset
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -3015,7 +4605,6 @@
                                     type: string
                                 enforcement:
                                     enum:
-                                        - RULESET_ENFORCEMENT_UNSPECIFIED
                                         - active
                                         - evaluate
                                         - disabled
@@ -3023,7 +4612,6 @@
                                     format: enum
                                 kind:
                                     enum:
-                                        - RULESET_KIND_UNSPECIFIED
                                         - merge_branch
                                         - push_branch
                                         - push_tag
@@ -3060,26 +4648,50 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Ruleset'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}/rulesets/{rulesetId}:
+    /v1/origin/repos/{ownerSlug}/{repoName}/rulesets/{rulesetId}:
         get:
             tags:
                 - OriginService
             description: Returns a single repository ruleset by its stable Origin id.
             operationId: OriginService_GetRuleset
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -3098,6 +4710,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Ruleset'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -3113,13 +4749,13 @@
                  ruleset with server-assigned rule and bypass-actor ids.
             operationId: OriginService_UpdateRuleset
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -3147,7 +4783,6 @@
                                     type: string
                                 enforcement:
                                     enum:
-                                        - RULESET_ENFORCEMENT_UNSPECIFIED
                                         - active
                                         - evaluate
                                         - disabled
@@ -3155,7 +4790,6 @@
                                     format: enum
                                 kind:
                                     enum:
-                                        - RULESET_KIND_UNSPECIFIED
                                         - merge_branch
                                         - push_branch
                                         - push_tag
@@ -3192,6 +4826,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Ruleset'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -3204,13 +4862,13 @@
             description: Deletes a repository ruleset by its stable Origin id.
             operationId: OriginService_DeleteRuleset
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -3226,13 +4884,37 @@
                 "204":
                     description: OK
                     content: {}
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{identifier.ownerSlug}/{identifier.name}:syncMirror:
+    /v1/origin/repos/{ownerSlug}/{repoName}:syncMirror:
         post:
             tags:
                 - OriginService
@@ -3247,13 +4929,13 @@
                  Repositories that do not pull from an upstream source are rejected.
             operationId: OriginService_SyncMirror
             parameters:
-                - name: identifier.ownerSlug
+                - name: ownerSlug
                   in: path
                   description: Owning entity's unique slug.
                   required: true
                   schema:
                     type: string
-                - name: identifier.name
+                - name: repoName
                   in: path
                   description: Repo name, unique to the owner entity.
                   required: true
@@ -3291,83 +4973,30 @@
                         application/json:
                             schema:
                                 $ref: '#/components/schemas/SyncMirrorResponse'
-                default:
-                    description: Default error response
-                    content:
-                        application/json:
-                            schema:
-                                $ref: '#/components/schemas/Status'
-    /v1/origin/repos/{ownerSlug}:
-        get:
-            tags:
-                - OriginService
-            description: Lists repos belonging to an owner entity.
-            operationId: OriginService_ListRepos
-            parameters:
-                - name: ownerSlug
-                  in: path
-                  description: Parent owner entity slug.
-                  required: true
-                  schema:
-                    type: string
-                - name: pageSize
-                  in: query
-                  description: |-
-                    Max repos to return. Defaults to 30 when unset or 0. Values above 100 are
-                     clamped to 100.
-                  schema:
-                    type: integer
-                    format: int32
-                - name: pageToken
-                  in: query
-                  description: |-
-                    Opaque cursor from a previous response's `next_page_token`. Empty for the
-                     first page.
-                  schema:
-                    type: string
-                - name: filter
-                  in: query
-                  description: Optional case-insensitive substring filter.
-                  schema:
-                    type: string
-            responses:
-                "200":
-                    description: OK
-                    content:
-                        application/json:
-                            schema:
-                                $ref: '#/components/schemas/ListReposResponse'
-                default:
-                    description: Default error response
-                    content:
-                        application/json:
-                            schema:
-                                $ref: '#/components/schemas/Status'
-        post:
-            tags:
-                - OriginService
-            description: Creates a repo belonging to an owner.
-            operationId: OriginService_CreateRepo
-            parameters:
-                - name: ownerSlug
-                  in: path
-                  description: Parent owner entity's slug.
-                  required: true
-                  schema:
-                    type: string
-            requestBody:
-                content:
-                    application/json:
-                        schema:
-                            $ref: '#/components/schemas/Repo'
-                required: true
-            responses:
-                "200":
-                    description: OK
-                    content:
-                        application/json:
-                            schema:
-                                $ref: '#/components/schemas/Repo'
+                "400":
+                    description: Bad Request
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "401":
+                    description: Unauthorized
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "403":
+                    description: Forbidden
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
+                "429":
+                    description: Too Many Requests
+                    content:
+                        application/json:
+                            schema:
+                                $ref: '#/components/schemas/Status'
                 default:
                     description: Default error response
                     content:
@@ -3465,7 +5094,6 @@
                 repoSelectionMode:
                     readOnly: true
                     enum:
-                        - APP_INSTALLATION_REPO_SELECTION_MODE_UNSPECIFIED
                         - all
                         - selected
                     type: string
@@ -3567,7 +5195,6 @@
                     description: The requested delivery id, echoed verbatim.
                 outcome:
                     enum:
-                        - OUTCOME_UNSPECIFIED
                         - queued
                         - already_in_flight
                         - not_found
@@ -3690,7 +5317,6 @@
                 status:
                     readOnly: true
                     enum:
-                        - CHECK_RUN_LIFECYCLE_STATUS_UNSPECIFIED
                         - queued
                         - in_progress
                         - completed
@@ -3699,7 +5325,6 @@
                 conclusion:
                     readOnly: true
                     enum:
-                        - CHECK_RUN_CONCLUSION_UNSPECIFIED
                         - success
                         - failure
                         - neutral
@@ -3752,6 +5377,11 @@
                     allOf:
                         - $ref: '#/components/schemas/CheckRunOutput'
                     description: Human-readable output for this check run, if set.
+                deadlineAt:
+                    readOnly: true
+                    type: string
+                    description: Optional deadline. Omitted or unset means no expiration.
+                    format: date-time
             description: |-
                 A persisted check run, as returned by `PostCheckRun`. All fields are
                  server-owned; the writable shape is `CheckRunInput`.
@@ -3767,7 +5397,6 @@
                 annotationLevel:
                     readOnly: true
                     enum:
-                        - ANNOTATION_LEVEL_UNSPECIFIED
                         - notice
                         - warning
                         - failure
@@ -3813,7 +5442,6 @@
             properties:
                 annotationLevel:
                     enum:
-                        - ANNOTATION_LEVEL_UNSPECIFIED
                         - notice
                         - warning
                         - failure
@@ -3876,7 +5504,6 @@
                     description: Human-facing check-run name.
                 status:
                     enum:
-                        - CHECK_RUN_LIFECYCLE_STATUS_UNSPECIFIED
                         - queued
                         - in_progress
                         - completed
@@ -3884,7 +5511,6 @@
                     format: enum
                 conclusion:
                     enum:
-                        - CHECK_RUN_CONCLUSION_UNSPECIFIED
                         - success
                         - failure
                         - neutral
@@ -3922,6 +5548,12 @@
                     allOf:
                         - $ref: '#/components/schemas/CheckRunOutput'
                     description: Human-readable output for this check run.
+                deadlineAt:
+                    type: string
+                    description: |-
+                        Optional deadline. Omitted or unset means no expiration.
+                         Must not be more than 24 hours in the future.
+                    format: date-time
             description: |-
                 A single check run to report. Combined with `CheckSuiteInput`, this is
                  upserted in one request.
@@ -4071,7 +5703,6 @@
             properties:
                 status:
                     enum:
-                        - COMMIT_COMPARISON_STATUS_UNSPECIFIED
                         - identical
                         - ahead
                         - behind
@@ -4357,7 +5988,6 @@
                     type: string
                 verdict:
                     enum:
-                        - PULL_REQUEST_REVIEW_VERDICT_UNSPECIFIED
                         - approve
                         - request_changes
                         - comment
@@ -4405,7 +6035,6 @@
                     type: string
                 enforcement:
                     enum:
-                        - RULESET_ENFORCEMENT_UNSPECIFIED
                         - active
                         - evaluate
                         - disabled
@@ -4413,7 +6042,6 @@
                     format: enum
                 kind:
                     enum:
-                        - RULESET_KIND_UNSPECIFIED
                         - merge_branch
                         - push_branch
                         - push_tag
@@ -4922,7 +6550,6 @@
                 repoSelectionMode:
                     readOnly: true
                     enum:
-                        - APP_INSTALLATION_REPO_SELECTION_MODE_UNSPECIFIED
                         - all
                         - selected
                     type: string
@@ -5653,7 +7280,6 @@
                 type:
                     readOnly: true
                     enum:
-                        - TYPE_UNSPECIFIED
                         - team
                         - user
                     type: string
@@ -5893,7 +7519,6 @@
                     description: The principal that authored the review.
                 verdict:
                     enum:
-                        - PULL_REQUEST_REVIEW_VERDICT_UNSPECIFIED
                         - approve
                         - request_changes
                         - comment
@@ -5955,7 +7580,6 @@
                     $ref: '#/components/schemas/PullRequestRequestedReviewer'
                 createdVia:
                     enum:
-                        - CREATED_VIA_UNSPECIFIED
                         - manual
                         - codeowners
                     type: string
@@ -6212,7 +7836,6 @@
                 source:
                     readOnly: true
                     enum:
-                        - SOURCE_UNSPECIFIED
                         - github
                     type: string
                     format: enum
@@ -6223,7 +7846,6 @@
                 status:
                     readOnly: true
                     enum:
-                        - STATUS_UNSPECIFIED
                         - inbound
                         - outbound
                     type: string
@@ -6332,7 +7954,6 @@
                     type: string
                 enforcement:
                     enum:
-                        - RULESET_ENFORCEMENT_UNSPECIFIED
                         - active
                         - evaluate
                         - disabled
@@ -6340,7 +7961,6 @@
                     format: enum
                 kind:
                     enum:
-                        - RULESET_KIND_UNSPECIFIED
                         - merge_branch
                         - push_branch
                         - push_tag
@@ -6397,7 +8017,6 @@
                     description: Stable Origin id for this bypass actor row.
                 bypassMode:
                     enum:
-                        - RULESET_BYPASS_MODE_UNSPECIFIED
                         - always
                         - pull_request_only
                     type: string
@@ -6421,7 +8040,6 @@
             properties:
                 bypassMode:
                     enum:
-                        - RULESET_BYPASS_MODE_UNSPECIFIED
                         - always
                         - pull_request_only
                     type: string
@@ -6442,7 +8060,6 @@
             properties:
                 role:
                     enum:
-                        - ROLE_UNSPECIFIED
                         - namespace_admin
                         - repository_admin
                         - repository_write
@@ -6700,7 +8317,6 @@
                     type: string
                 enforcement:
                     enum:
-                        - RULESET_ENFORCEMENT_UNSPECIFIED
                         - active
                         - evaluate
                         - disabled
@@ -6708,7 +8324,6 @@
                     format: enum
                 kind:
                     enum:
-                        - RULESET_KIND_UNSPECIFIED
                         - merge_branch
                         - push_branch
                         - push_tag
@@ -6799,7 +8414,6 @@
                 trigger:
                     readOnly: true
                     enum:
-                        - WEBHOOK_DELIVERY_TRIGGER_UNSPECIFIED
                         - automatic
                         - manual
                     type: string
@@ -6842,7 +8456,6 @@
                     $ref: '#/components/schemas/Owner'
                 repoSelectionMode:
                     enum:
-                        - APP_INSTALLATION_REPO_SELECTION_MODE_UNSPECIFIED
                         - all
                         - selected
                     type: string
@@ -6874,5 +8487,12 @@
                     allOf:
                         - $ref: '#/components/schemas/OriginUserActor'
                     description: User who originally installed the app.
+    securitySchemes:
+        bearerAuth:
+            type: http
+            description: 'Origin API credentials are sent as `Authorization: Bearer` tokens: an app JWT for app-level endpoints, or an installation access token (`oit_...`) for installation-scoped endpoints.'
+            scheme: bearer
+security:
+    - bearerAuth: []
 tags:
     - name: OriginService
````
